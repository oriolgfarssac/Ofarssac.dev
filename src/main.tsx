import React from 'react'
import ReactDOM from 'react-dom/client'
import AppP from './appP.tsx'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <AppP />
  </React.StrictMode>,
)
